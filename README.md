# موقع جمعية تازة للمستقبل (Association Taza Dashboard)

تطبيق ويب مبني باستخدام **Flask** لإدارة أنشطة وأخبار الجمعية، مع لوحة تحكم كاملة للمسؤولين.

## المميزات المطورة 🚀
- **لوحة تحكم احترافية (Admin Dashboard)**: لإدارة المحتوى بالكامل.
- **إدارة الأخبار والأنشطة**: إضافة، تعديل، وحذف المقالات والأنشطة مع رفع الصور.
- **معرض الصور (Gallery)**: لوحة خاصة لرفع صور الجمعية وعرضها.
- **إدارة الرسائل**: تتبع رسائل "اتصل بنا" وطلبات التطوع.
- **تصميم وتواصل**: نظام بريدي متكامل وتصميم متجاوب يدعم العربية.

## دليل المسؤول (Admin Guide) 🔐
للوصول للوحة التحكم، استخدم الرابط التالي في موقعك:
`your-site.vercel.app/admin`

**بيانات الدخول الافتراضية:**
- **اسم المستخدم**: `admin`
- **كلمة المرور**: `admin123`

- Python 3.10+
- Flask 3.0.2

## طريقة التشغيل محلياً (Installation)
1. قم بتحميل المستودع:
   ```bash
   git clone https://github.com/YOUR_USERNAME/assosiation-taza.git
   cd assosiation-taza
   ```
2. قم بإنشاء بيئة افتراضية:
   ```bash
   python -m venv venv
   source venv/bin/activate  # على Windows: venv\Scripts\activate
   ```
3. تثبيت المكتبات اللازمة:
   ```bash
   pip install -r requirements.txt
   ```
4. تشغيل التطبيق:
   ```bash
   python run.py
   ```

## ملاحظة أمنية
يستخدم هذا المشروع ملف `.gitignore` لمنع رفع ملفات قاعدة البيانات الخاصة (`site.db`) والمفاتيح السرية إلى الإنترنت.
